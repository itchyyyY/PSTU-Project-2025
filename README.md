Этот проект написан с использованием фреймворка **Qt** и языка **C++**.

Требования:
- Qt (6.x)
- g++ (GNU C++ Compiler)

**Важно:** Для корректной работы приложения `gcc` и `g++` должны быть добавлены в переменную среды `PATH`.
